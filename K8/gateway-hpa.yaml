apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
    name: gateway-hpa
spec:
    scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: gateway
    minReplicas: 1
    maxReplicas: 20
    targetCPUUtilizationPercentage: 50
